# 数据库迁移

## v0.1.0

默认数据库为 `data/gateway.db`。首次启动 v0.1.0 时，应用会在同一 SQLite 文件内自动完成兼容迁移。

迁移可能执行以下操作：

- 为路由补充站点名称、备注和线路检测状态字段。
- 移除本地模型名的唯一约束，允许多个站点共享同一别名。
- 创建网关设置表和使用记录表。
- 为使用记录补充站点、请求类型、流式标识、首字耗时等字段。

迁移保留以下数据：

- 模型路由及启用状态
- 上游 API Key
- 页面生成的网关 API 令牌
- 现有使用记录

## 升级步骤

1. 停止正在运行的网关。
2. 复制 `data/gateway.db` 到安全位置。该文件包含明文 API Key，不能提交到 Git 或上传到公开位置。
3. 更新代码并安装依赖。
4. 使用 `.\scripts\start.ps1` 启动网关。
5. 打开管理页面，确认路由、令牌状态和使用记录正常。

迁移在应用创建数据库连接时自动执行，不需要手工运行 SQL。若启动失败，请保留原数据库和备份文件，不要反复删除或覆盖数据库。
